#!/bin/bash
# Make local repo in sync with upstream

REMOTE_NAME="percona"
git fetch ${REMOTE_NAME}
for branch in 5.6 5.7 8.0;
do
  git checkout ${branch} &&
  git merge ${REMOTE_NAME}/${branch} &&
  git push origin
done
